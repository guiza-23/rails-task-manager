<h2><%= @task.title %></h2>
<p><%= @task.details  %></p>
<p><%= @task.completed %></p>
<p><%= link_to "edit", edit_path %></p>
<p><%= link_to "remove", destroy_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>


<p><%= link_to "back to tasks", tasks_path %></p>
